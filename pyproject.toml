[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "diffusionrl"
dynamic = ["version"]
description = "Diffusion RL training framework"
readme = "docs/Diffusion_RL_Design_Analysis.md"
requires-python = ">=3.10"
license = { file = "LICENSE" }
authors = [
  { name = "diffusionrl contributors" }
]
dependencies = [
  "numpy>=1.24,<3",
  "torch>=2.1",
  "ray[default]>=2.9,<3",
  "sglang[diffusion] @ git+https://github.com/celve/sglang.git@diffusionrl#subdirectory=python",
  "diffusers>=0.30",
  "hydra-core>=1.3",
  "omegaconf>=2.3",
  "transformers>=4.41",
  "peft>=0.11",
  "safetensors>=0.4",
  "Pillow>=10",
  "requests>=2.31",
  "psutil>=5.9",
  "tensordict>=0.5",
]

[project.optional-dependencies]
train = [
  "wandb>=0.16,<0.20",
  "aiohttp>=3.9",
]
infer = [
  "accelerate>=0.30",
]
eval = [
  "torchvision>=0.16",
  "easyocr>=1.7",
]
dev = [
  "pytest>=7.4",
  "pytest-cov>=4.1",
  "ruff>=0.6",
  "pre-commit>=3.6",
]

[tool.setuptools]
include-package-data = true

[tool.setuptools.dynamic]
version = { attr = "diffusionrl.__version__" }

[tool.setuptools.packages.find]
where = ["."]
include = [
  "diffusionrl",
  "diffusionrl.*",
]

[tool.ruff]
line-length = 120
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
ignore = ["E501"]

[tool.ruff.lint.isort]
known-first-party = ["diffusionrl"]

[tool.ruff.format]
quote-style = "double"

[tool.pytest.ini_options]
testpaths = ["tests"]
norecursedirs = ["_dev"]
pythonpath = ["."]
# importlib mode avoids stdlib shadowing when subdirs use names like ``types``
# that collide with Python's stdlib during diffusionrl.types submodule resolution.
addopts = "--import-mode=importlib"
markers = [
  "e2e: heavy end-to-end tests that are skipped unless --run-e2e is provided",
  "gpu: tests that require GPU resources",
  "slow: tests that are slow to execute",
]
