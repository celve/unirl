"""New-tier model package.

Holds the typed pipeline-stage protocols (``models.types``) that pair
with the four-tier type system (``Conditions``, ``Segments``, packed
primitives, ``RolloutReq`` / ``RolloutResp``). Concrete bundle migrations
will land here as they convert from the legacy ``unirl.models.*``
shape.
"""
